import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  main: {
    padding: '1.5rem',
    width: '100%',
    height: '100%',
  },
  container: {},
}));

export default useStyles;
